# Frontend runtime config (used at build-time by Vite)
# In local dev you can keep empty to use Vite proxy (/api -> http://127.0.0.1:8787)
# In Netlify set this to your deployed API domain.
VITE_API_BASE_URL=
